# Base URL of the MarMa HRIS API.
# Leave as /api in development: vite proxies it to VITE_API_PROXY_TARGET.
VITE_API_BASE_URL=/api/v1

# Only used by the dev server proxy.
VITE_API_PROXY_TARGET=http://localhost:3000

# Shown in the sign-in screen and the window title.
VITE_APP_NAME=MarMa HRIS
